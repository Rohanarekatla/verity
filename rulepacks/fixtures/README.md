# rulepacks/fixtures/

Passing and failing HTML fixtures, one pair per rule in
[`rulepacks/`](../), proving each custom rule actually discriminates
between the two before it's trusted in CI.

Not yet populated.

# 2026-W04 — Sunday teach-back

Week 4 · 7–13 Sep · keyboard traversal and trap detection.

**The listener writes the notes, not the teacher.** See
[`../team-plan.md`](../team-plan.md) §2.3.

> Scaffolded ahead of the session. B's half is a stub because Track A's
> traversal had not landed at the time of writing — fill it in on Sunday.

---

## A → B: tab-order capture and multi-cycle trap detection

*Notes to be written by B (Nikhil), after the session*

Ground to cover:

- How the traversal walks focus, and what it does at the document boundary
- How shadow DOM and iframes are handled, or reported unvisited
- **How N was determined by measurement** for multi-cycle trap detection —
  the plan requires the number to come from data and be recorded in
  `docs/measurements/`, so the interesting question is what the measurement
  actually showed
- How the focus-visible before/after diff decides "there is an indicator",
  and what it does with an indicator drawn as a `box-shadow` rather than an
  `outline`
- How the APG contract loader validates a contract file

---

## B → A: three-state outcomes, and why a pass is harder than a fail

*Notes to be written by A (Rohan), after the session*

Ground to cover:

- Why keyboard judgments are three-state, and what `indeterminate` maps to
- **Evidence of a defect stands alone; absence of evidence needs complete
  coverage** — why a `fail` can be asserted from a partial traversal but a
  `pass` cannot
- Why `focus_visible=None` and `focus_visible=False` must not be collapsed
- What `ARROW_NAVIGABLE` is for, and why a correct tablist shows five
  elements Tab never lands on
- Why the retry policy never escalates to `fail` when the attempts run out

---

## The thing worth discussing at the session

The `ARROW_NAVIGABLE` reason code did not exist until the tablist APG contract
was written. Writing the contract *before* the runner code — which the plan
required and which we nearly skipped — is what surfaced it. Without it the
checker would have reported five unreachable tabs on the first APG reference
implementation we pointed it at, and failed the gate on a widget built exactly
to spec.

Worth deciding jointly: is "write the contract first" a rule we keep for every
widget pattern in Weeks 5+, or was this a one-off?

---

## Thin spots to revisit

- Still neither of us has explained the other's test strategy. Third week
  running.
- `2026-W01.md` remains unwritten.
